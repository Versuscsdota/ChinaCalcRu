# Normalize line endings to LF in repo and working tree (prevents CRLF warnings on Windows)
* text=auto eol=lf

# Treat common binary formats as binary explicitly
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.svg text eol=lf

# Optional: keep Windows line endings for batch/powershell scripts
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
